namespace ACSE.Core.Enums
{
    // TODO: Split all enums into their own files.
    public enum AnimalCrossingGrassType : byte
    {
        Triangle = 0,
        Square = 1,
        Circle = 2
    }

    public enum WildWorldGrassType : byte // NOTE: Wild World saves the grass's seasonal modifier. Going to have to document all of them.
    {
        Triangle = 0,
        Circle = 1,
        Square = 2
    }

    public enum CityFolkGrassType : byte
    {
        Triangle = 0,
        Circle = 1,
        Square = 2
    }

    public enum NewLeafGrassType : byte
    {
        Triangle = 0,
        Circle = 1,
        Square = 2
    }
}